apiVersion: v1
kind: Service
metadata:
  name: lunch-swap0
  labels:
    app.kubernetes.io/name: lunch-swap0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lunch-swap0
  ports:
    - name: http
      port: 80
      targetPort: http
